import 'package:flutter/material.dart';

class RatingProductsScreen extends StatelessWidget {
  const RatingProductsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold();
  }
}
